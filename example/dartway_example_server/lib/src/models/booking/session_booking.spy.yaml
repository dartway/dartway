class: SessionBooking
table: session_booking
fields:
  # Named clubSession (not `session`): Serverpod's generated helpers already
  # take a `session` parameter, a relation with that name would collide.
  clubSession: ClubSession?, relation
  clientProfile: UserProfile?, relation
  status: BookingStatus
  createdAt: DateTime
