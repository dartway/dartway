.dart_tool/

# FVM Version Cache
.fvm/

# Build outputs (generated)
build/

# Flutter tool generated
.flutter-plugins
.flutter-plugins-dependencies

# Claude Code — внутренняя кухня разработки фреймворка (не для публичного монорепо)
.claude/settings.local.json
.claude/skills/
/CLAUDE.md
# Project management — private nested repo (dartway_manager)
project/

# dartway.dev website — public nested repo (novikov-it/dartway.dev)
website/

# DartWay Studio platform — private nested repo (dartway/dartway_studio)
platform/

# custom_lint run artifact
custom_lint.log
