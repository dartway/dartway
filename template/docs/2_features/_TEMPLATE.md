---
code-anchors:
  - dartway_starter_server/lib/src/models/<feature>/
  - dartway_starter_server/lib/src/crud/<feature>_crud_config.dart
  - dartway_starter_flutter/lib/app/<feature>/
last-verified: 2000-01-01
---

# <Feature name>

> Copy this file to `<feature>.md` when you build a feature. `code-anchors` are
> the code paths this feature owns — `dartway-finish` uses them to detect when
> the doc has drifted from the code. Update `last-verified` when you re-check it.

## What it does

One paragraph: the user-facing behaviour.

## How it works

The end-to-end flow — models, the `DwCrudConfig`, the screen and how it reads
the data. Name the moving parts.

## Rules & edge cases

Access rules (who sees / writes what), validations, race-safety, and anything
non-obvious a future reader would trip on.
