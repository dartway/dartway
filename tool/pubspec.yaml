name: dartway_repo_tools
description: The scripts this repository runs on itself, and the logic they judge it with.
publish_to: none
resolution: workspace
environment:
  sdk: ^3.11.0
dependencies:
  dartway_cli: ^0.13.0
  yaml: ^3.1.2
dev_dependencies:
  test: ^1.25.0
