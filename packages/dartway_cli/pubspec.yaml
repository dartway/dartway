name: dartway_cli
description: "DartWay command-line tool: create projects from the canonical template, install the AI toolkit, run convention checks."
version: 0.1.1
repository: https://github.com/dartway/dartway
homepage: https://dartway.dev
issue_tracker: https://github.com/dartway/dartway/issues
topics: [dartway, cli, serverpod, flutter, fullstack]
resolution: workspace

environment:
  sdk: ^3.11.0

executables:
  dartway:

dependencies:
  args: ^2.6.0
  path: ^1.9.0
