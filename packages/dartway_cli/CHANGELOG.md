# Changelog

## 0.1.0

- Initial release: `dartway create` (project from the canonical template), `dartway setup-ai` (AI toolkit install/update with managed-files-only overwrite), `dartway check` (convention checks via dartway_code_checker).
