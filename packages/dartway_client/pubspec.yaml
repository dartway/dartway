name: dartway_client
description: The DartWay client — HTTP calls, the live update socket, request state and sessions. Pure Dart.
version: 0.21.0-dev.7
repository: https://github.com/dartway/dartway
license: Apache-2.0
resolution: workspace
environment:
  sdk: ^3.11.0
dependencies:
  dartway_core_shared: ^0.21.0-dev.7
  http: ^1.6.0
  web: ^1.1.1
  web_socket_channel: ^3.0.3
dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
