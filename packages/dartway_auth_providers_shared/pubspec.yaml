name: dartway_auth_providers_shared
description: The contract of signing in with Google and Apple, shared by a DartWay server and its app — the command, the providers, and the refusals.
version: 0.1.0
repository: https://github.com/dartway/dartway
license: Apache-2.0
resolution: workspace
environment:
  sdk: ^3.11.0
dependencies:
  dartway_core_shared: ^0.20.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
