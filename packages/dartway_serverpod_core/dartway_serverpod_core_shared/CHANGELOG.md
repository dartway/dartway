# Changelog

## 0.1.0

- First public release: `DwAlerts` with Telegram delivery, `DwAlertContext` +
  `DwAlertFormatter` (MarkdownV2-safe messages, structured app context,
  trimmed stack traces), configuration keys and shared utilities.
