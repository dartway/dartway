include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**

# The plugin this fixture proves, by path — a project names a version instead.
plugins:
  dartway_lints:
    path: ../
