# Changelog

## 0.1.0

- First public release: `forbidden_ui_style_usage` — flags raw `Color`,
  `TextStyle`, `BorderRadius` constructions, `Colors.*` and
  `context.textTheme`/`colorScheme` access outside `ui_kit/`.
