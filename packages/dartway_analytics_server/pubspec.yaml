name: dartway_analytics_server
description: Analytics for a DartWay server — events from the app stored in the project's own Postgres in batches, sessions, events recorded by the server itself, and retention.
version: 0.1.0
repository: https://github.com/dartway/dartway
license: Apache-2.0
resolution: workspace
environment:
  sdk: ^3.11.0
dependencies:
  crypto: ^3.0.6
  dartway_analytics_shared: ^0.1.0
  dartway_core_server: ^0.21.0-dev.3
  meta: ^1.16.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
