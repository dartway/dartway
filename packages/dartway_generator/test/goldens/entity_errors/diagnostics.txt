app_server/lib/src/columns.dart:15:13: an index of row class `ColumnsRow` names `missing`, which is not a column field of `ColumnsRow` (indexes name Dart fields, not SQL columns)
app_server/lib/src/columns.dart:15:13: row class `ColumnsRow` declares index `columns_title_idx` twice
app_server/lib/src/columns.dart:30:19: field `view` of row class `ColumnsRow` cannot be a column: `ValueView` is a DTO, which is not stored; store its fields as columns (or ids of other rows) and map it in the handler
app_server/lib/src/columns.dart:31:24: field `dates` of row class `ColumnsRow` cannot be a column: `List<DateTime>` is stored as jsonb, whose elements are read back as plain JSON: they must be int, double, String or bool — or, in a list, a non-null enum
app_server/lib/src/columns.dart:32:18: field `set` of row class `ColumnsRow` cannot be a column: `Set<int>` cannot be stored; supported: int, double, String, bool, DateTime, Duration, Uint8List, an enum, List<T> or Map<String, T> of int, double, String or bool (jsonb), List<E> of an enum (jsonb of names), or a nullable one of these
app_server/lib/src/columns.dart:35:16: field `ownerName` of row class `ColumnsRow` references another table, so it holds that row's id and must be `int` or `int?`
app_server/lib/src/columns.dart:40:13: fields `same` and `alsoSame` of row class `ColumnsRow` both map to column `same`
app_server/lib/src/columns.dart:46:13: table name `shared_name` of row class `SameTableARow` is also used at columns.dart:56:13
app_server/lib/src/columns.dart:56:13: table name `shared_name` of row class `SameTableBRow` is also used at columns.dart:46:13
app_server/lib/src/columns.dart:68:13: the table name `a_table_name_that_is_far_too_long_for_postgres_to_keep_in_one_piece` is longer than 63 bytes, and Postgres would silently truncate it
app_server/lib/src/declarations.dart:5:13: row class `NoTableRow` needs its table: annotate it with `@DwSqlTable('no_table')`
app_server/lib/src/declarations.dart:15:13: row class `NoStaticTableRow` must declare its table: add `static const tableDef = NoStaticTableTable();`
app_server/lib/src/declarations.dart:27:13: the id of row class `WrongIdRow` must be `int?` (a bigserial key, null before insert), not `int`
app_server/lib/src/declarations.dart:42:16: field `tableName` of row class `ShadowingRow` would shadow `DwTableDef.tableName` in the generated table class; rename the field (the column can keep its name with `@DwColumnName('...')`)
app_server/lib/src/declarations.dart:43:13: field `tableColumns` of row class `ShadowingRow` would shadow `DwTableDef.tableColumns` in the generated table class; rename the field (the column can keep its name with `@DwColumnName('...')`)
app_server/lib/src/declarations.dart:55:13: field `tableDef` of row class `TableDefFieldRow` would clash with the `static const tableDef` every row class declares; rename the field (the column can keep its name with `@DwColumnName('...')`)
app_server/lib/src/declarations.dart:60:13: row class `ClubSession` must be named `<Entity>Row` (`ClubSessionRow`): its table class is `<Entity>Table` and its repository getter `db.<entities>`, and a row never shares a name with the data object clients see
app_server/lib/src/declarations.dart:71:13: row class `Row` must be named `<Entity>Row` (`SomeRow`): its table class is `<Entity>Table` and its repository getter `db.<entities>`, and a row never shares a name with the data object clients see
app_server/lib/src/prefixed.dart:6:13: the generated part uses `DwFieldPatch`, `DwTableColumn`, `DwTableDef`, `DwResultRow`, which `prefixed.dart` does not import without a prefix; add `import 'package:dartway_orm/dartway_orm.dart';`
app_shared/lib/misplaced.dart:6:13: row class `MisplacedRow` is declared in `app_shared`; row classes belong in the *_server package, whose schema lists them
