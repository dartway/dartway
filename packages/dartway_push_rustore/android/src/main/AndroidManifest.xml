<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <!-- flutter_rustore_push registers a messaging service of its own. This
             plugin replaces it with one that also persists the payload for the
             tap and draws a notification for data-only messages, which the SDK
             leaves invisible. Both cannot be registered: RuStore delivers to
             one. -->
        <service
            android:name="ru.rustore.flutter_rustore_push.FlutterRustorePushService"
            tools:node="remove" />

        <service
            android:name="dev.dartway.push.rustore.DwRuStorePushService"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="ru.rustore.sdk.pushclient.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
